[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Skip the Next.js plugin - this is a Vite project
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

# SPA routing - redirect all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
